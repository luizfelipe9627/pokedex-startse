const getPokemonUrl=a=>`https://pokeapi.co/api/v2/pokemon/${a}`,fetchPokemon=()=>{let b=[];for(let a=1;a<=151;a++)b.push(fetch(getPokemonUrl(a)).then(a=>a.json()));Promise.all(b).then(a=>{let b=a.reduce((c,a)=>{let b=a.types.map(a=>a.type.name);return c+`
        <li class="card ${b[0]}">
          <img class="card-image" alt="${a.name}"
          src="https://raw.githubusercontent.com/PokeAPI/sprites/master/sprites/pokemon/${a.id}.png">
          <h2 class="card-title">${a.id}. ${a.name}</h2>
          <p class="card-subtitle">${b.join(" | ")}</p>
        </li>
      `},""),c=document.querySelector('[data="pokedex"]');c.innerHTML=b})};fetchPokemon()